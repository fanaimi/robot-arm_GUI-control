/**
 * @name    main.js
 * @desc    takes care of sliders
 */

(function(){
var



    init = function(){
        console.log("foo");
    }// init
;

window.addEventListener("load", init);
//$(document).on("ready", init);
})();
